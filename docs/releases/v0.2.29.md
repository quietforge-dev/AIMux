## 新功能

- 账号管理支持将账号配置复制为 JSON，并在新增账号时从 JSON 导入，便于在多台设备间迁移账号配置。

## 优化与修复

- 检查更新时会展示加载状态、旋转图标和进行中提示，避免重复点击时缺少反馈。

## 升级说明

- 账号 JSON 包含 API Key，仅应通过可信渠道保存和传输；导入后仍需确认并保存账号。
- 本版本不涉及数据库结构变更，现有数据可直接继续使用。
