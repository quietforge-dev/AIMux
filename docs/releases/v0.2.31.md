## 新功能

- 账号新增和编辑时支持从上游接口查询模型列表，在弹窗中筛选、勾选后回填支持模型。
- 查询兼容 OpenAI 与 Anthropic 鉴权方式；已选但本次接口未返回的模型可继续保留或取消。

## 优化与修复

- 优化账号新增与编辑弹窗布局，基础信息与常用字段采用更紧凑的栅格排布，减少垂直滚动。

## 升级说明

- 查询结果取决于上游服务和当前 API Key 的模型列表能力；不支持该接口时仍可手动维护支持模型。
- 本版本不涉及数据库结构变更，现有数据可直接继续使用。
