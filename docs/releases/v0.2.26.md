## 优化与修复

- 模型维护删除模型后立即刷新当前列表，并在删除结果成功或失败时给出明确提示。
- 兼容 Anthropic 响应中的缓存 Token 字段：支持缓存读取、缓存写入及 5 分钟、1 小时缓存写入细分字段。
- 统一 OpenAI 与 Anthropic 的输入 Token、缓存 Token 和缓存率统计口径，数据统计与使用记录可正确展示 Anthropic 缓存命中情况。

## 升级说明

- 本版本不涉及数据库结构变更，现有数据可直接继续使用；升级后产生的 Anthropic 使用记录将按新口径统计。
