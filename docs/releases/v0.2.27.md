## 新功能

- 监控管理页面新增账号监控开关，可在刷新按钮旁直接启用或关闭监控。
- 兼容 Anthropic 的 `output_config.effort` 和顶层 `effort` 推理强度字段，使用记录可保存 `high`、`medium`、`low` 等值。

## 优化与修复

- 账号监控开关使用独立更新接口，立即更新运行中的后台监控状态，不会覆盖其他设置。
- 设置页面保存其他配置时保留当前账号监控状态。

## 升级说明

- 本版本不涉及数据库结构变更，现有数据可直接继续使用。
