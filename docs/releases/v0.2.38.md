## 优化与修复

- 账号管理列表的“测试默认模型”现在会在模型名称前显示对应的供应商 Logo。
- 监控管理列表的“测试模型”现在会在模型名称前显示对应的供应商 Logo，方便快速识别 OpenAI、Anthropic、DeepSeek、Moonshot AI 等模型来源。
- 供应商根据模型的协议类型和名称从模型目录中匹配，避免不同协议下同名模型显示错误的 Logo。
- 自定义模型、已删除模型或模型目录中无法匹配的模型仍会正常显示名称，不会因为缺少供应商信息影响列表使用。

## 升级说明

- 本次更新不修改账号表或监控记录表，不需要执行新的数据库迁移。
