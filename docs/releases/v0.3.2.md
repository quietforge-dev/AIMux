## 优化与修复

- 新增倍率监控配置时，账号待选区会自动排除已经被其他倍率监控配置使用的账号，避免同一个账号被多个启用配置重复同步。
- 编辑倍率监控配置时，仍会保留当前配置已选择的账号；只有其他配置正在使用的账号会被排除，方便安全调整当前配置。
- 账号选择弹窗会显示被其他配置排除的账号数量，当前可选数量和已选数量更加清晰。

## 升级说明

- 本次更新仅调整倍率监控页面的前端账号选择逻辑，不修改数据库结构和后端接口。
- 现有倍率监控配置、账号倍率和监控日志无需迁移。
