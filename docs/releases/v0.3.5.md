## 新功能

- OpenAI 默认模型目录新增 GPT-6 Sol（`gpt-6-sol`）和 GPT-6 Luna（`gpt-6-luna`）。
- Anthropic 默认模型目录新增 Claude Opus 5.5（`claude-opus-5-5`）。

## 升级说明

- 升级后会自动补充上述缺失的内置模型，不会覆盖或删除用户自行维护的模型，也不会改变用户当前的测试默认模型设置。
