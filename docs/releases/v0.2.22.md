## 新功能

- 数据统计的启用账号列表新增“最近20次缓存率”，按每个账号今日最近 20 条有效使用记录的输入 Token 加权计算，并显示实际样本数。

## 优化与修复

- 账号手动测试和定时监控统一使用相同的测试模型选择、模型映射和上游探测请求，减少两种检测结果不一致的情况。
- 使用记录的列表和汇总统一复用筛选条件，保证筛选后的统计口径与列表一致。
- 上游失败响应统一进行安全截断，避免过长响应内容影响日志和错误展示。

## 升级说明

- 本版本不涉及数据库结构变更，现有数据可直接继续使用。
